//! `RegistrySvc` — zero-size factory struct.

/// Zero-size service struct whose associated functions serve as the public
/// factory entry points for this crate.
///
/// All functionality is exposed via `saf/registry_svc.rs`; this type acts
/// as a namespace for the implementation methods defined there.
pub struct RegistrySvc;
